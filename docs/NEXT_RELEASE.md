# التحديث القادم: 1.4.0

## حدود العزل

- الإصدار 1.3 (البناء 9) يبقى ثابتًا على `codex/release-1.3.0` وtag `v1.3.0-build.9`.
- كل عمل جديد يتم على `codex/develop-1.4.0` أو فروع ميزة خارجة منه.
- لا يُعاد نشر Replit production أثناء تطوير 1.4؛ يستخدم staging وfeature flags فقط.
- لا يُرفع أي Archive باسم 1.4 قبل اكتمال Release Candidate وقائمة التحقق.

## نقطة البداية

- Marketing Version المقترح: `1.4`.
- Package Version المقترح: `1.4.0`.
- أول build مقترح: `10` للحفاظ على تسلسل تشغيلي واضح.
- App Store record لا يُنشأ إلا عند تجميد نطاق 1.4.

## قالب تخطيط كل ميزة

| الحقل | المطلوب |
| --- | --- |
| مشكلة العميل | وصف قابل للقياس |
| معيار القبول | نتيجة يمكن اختبارها |
| التحليلات | الحد الأدنى من الأحداث دون بيانات شخصية |
| الخصوصية | بيانات جديدة، مدة الاحتفاظ، والحذف |
| التوافق | سلوك 1.3 عند التعامل مع الخادم الجديد |
| التحكم | feature flag وخطة rollout |
| الرجوع | طريقة تعطيل الميزة أو استعادة الخادم |

## البوابات

1. تصميم وموافقة نطاق.
2. اختبارات تلقائية ومراجعة كود.
3. staging وبيانات اختبار منفصلة.
4. TestFlight داخلي.
5. اختبار قبول على أجهزة حقيقية.
6. Release Candidate مجمّد.
7. نشر خادم متوافق ثم App Store.

أي فكرة لا تملك معيار قبول وخطة رجوع تبقى Issue ولا تدخل فرع الإصدار.
