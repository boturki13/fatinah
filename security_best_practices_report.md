# تقرير تدقيق الأمان — فطنة

تاريخ التحديث: 2026-09-05

## الخلاصة التنفيذية

لم يظهر في النطاق المفحوص عيب مؤكد عالي أو حرج. أُغلق **SEC-01** على مستوى الكود والاختبارات المحلية: لم يعد `script-src` يسمح بـ`'unsafe-inline'` أو `'unsafe-eval'`، ومُنع تنفيذ سمات الأحداث عبر `script-src-attr 'none'`. بقي السماح بالأنماط المضمنة في `style-src` بصورة صريحة؛ هذا لا يعيد السماح بتنفيذ JavaScript، لكنه يظل تحصينًا ناقصًا موثقًا أدناه.

أعيد تشغيل تدقيقي اعتماديات الإنتاج في الحزمة الرئيسية وCloud Functions، وكانت النتيجة صفر ثغرات معروفة. كما نجحت اختبارات CSP وHTTP/304 واختبار مشغّل الخادم محليًا. عُزّزت بوابات CI في ملفات workflow، لكن **لم تُنفّذ على GitHub بعد**؛ لا يعني هذا التقرير أن نسخة نُشرت أو رُفعت إلى TestFlight.

## النتائج

### SEC-01 — تنفيذ السكربت المضمن في CSP

- الخطورة الأصلية: متوسطة.
- الحالة: **تم الإصلاح محليًا**.
- التنفيذ: تحدد سياستا الويب والصفحات القانونية `script-src` بمصادر صريحة، وتحددان `script-src-attr 'none'` (`server.py:29-52`). تُرسل السياسة كـHTTP header على `/` و`/index.html` وعلى `/privacy-policy.html` و`/terms-of-service.html` وكل مسارات `/privacy` و`/terms` و`/legal` ذات الصلة (`server.py:4148-4252`).
- الواجهة: أزيلت معالجات الأحداث المضمنة واستُبدلت بمرسل أحداث ذي قائمة سماح، بلا `eval` أو تحويل أسماء نصية إلى دوال (`www/app.js:5803-5945`). سياسة meta في التطبيق تمنع سمات السكربت أيضًا (`www/index.html:8`)، وتبديل لغة الصفحات القانونية أصبح في ملف JavaScript ذاتي الاستضافة.
- التحقق: يختبر HTTP الفعلي السياسة على الصفحة الرئيسية والمسارات القانونية، ويثبت أن استجابة `304` لـ`/privacy` تحتفظ بالهيدر نفسه (`tests/test_free_round_reports_metrics.py:95-184`). كما يفحص الاختبار الثابت غياب inline scripts/handlers ومزامنة نسخة iOS (`tests/ios_privacy_config_test.mjs:188-220`, `tests/ios_privacy_config_test.mjs:257`). نجح الاختباران محليًا.

### SEC-02 — الثقة في بيانات `localStorage`

- الخطورة: منخفضة.
- الحالة: متبقية.
- الموقع: طبقة التخزين والاستعادة في `www/app.js:490-573`، مع استخدامات إضافية لاحقة للحالة المحلية.
- الأثر: يستطيع أي سكربت ينجح في العمل ضمن المصدر نفسه قراءة القيم أو تعديلها. لم يظهر تخزين كلمة مرور أو Firebase ID token في هذا المسار، لكن لا يجوز اعتبار حالة اللعبة أو التفضيلات أو cache الاشتراك مصدر صلاحية موثوقًا.
- الضابط المطلوب: إبقاء الخادم مصدر الحقيقة للصلاحيات والمدفوعات، والتحقق من الأنواع والحدود عند الاستعادة.

### SEC-03 — السماح بالأنماط المضمنة

- الخطورة: منخفضة.
- الحالة: متبقية ومقصودة للتوافق الحالي.
- الدليل: تحتوي سياستا HTTP على `style-src 'self' 'unsafe-inline'` (`server.py:36`, `server.py:47`)، وتسمح سياسة التطبيق بسمات style عبر `style-src-attr 'unsafe-inline'` (`www/index.html:8`).
- الأثر: يضعف ذلك احتواء حقن CSS أو التلاعب المرئي إذا وُجدت لاحقًا ثغرة حقن HTML، لكنه لا يسمح بتشغيل السكربت ولا يغيّر إغلاق SEC-01.
- التوصية: نقل سمات `style` المتبقية إلى classes، ثم اعتماد `style-src-attr 'none'` وإزالة `'unsafe-inline'` من `style-src` في تغيير مستقل مع اختبارات بصرية.

### SEC-R01 — اعتماد `qs` المعرض لحجب الخدمة

- الحالة: تم الإصلاح.
- الإجراء: تثبيت `qs >= 6.16.0` في `functions/package.json`؛ النسخة المقفلة الحالية `6.16.0`.
- التحقق الحالي: `npm audit --omit=dev` و`npm audit --prefix functions --omit=dev` أعادا صفر ثغرات معروفة.

## موثوقية الاختبارات وبوابات CI

- مشغّل الخادم يفرض مهلة افتراضية 120 ثانية لكل suite، ينقّي متغيرات بيئة التطبيق، يسجل `START/PASS/FAIL/TIMEOUT`، وينهي شجرة العمليات على POSIX وWindows مع معالجة `SIGINT` و`SIGTERM` (`scripts/run-server-tests.mjs:8-47`, `scripts/run-server-tests.mjs:140-173`, `scripts/run-server-tests.mjs:220-388`).
- اختبار regression يثبت النجاح العادي، انتهاء المهلة، وقتل parent وgrandchild عنيدين (`tests/run_server_tests_runner_test.mjs:59-143`)، وهو جزء من `npm run test:server` (`package.json:90`). نجح الاختبار ونجحت جميع server suites محليًا.
- بوابة CI الأساسية تشغل اختبارات static والمصادر الحتمية والمحتوى والحساب واللعبة والواجهة والخادم، وتفصل فحص أصول الإصدار للوسوم والتشغيل اليدوي (`.github/workflows/ci.yml:20-104`).
- بوابة iOS تدعم الوسوم والتشغيل اليدوي، وتثبت Chromium، وتتحقق من محتوى الأسئلة والأصول، وتشغل الاختبارات والتدقيقات، ثم Xcode tests وunsigned archive (`.github/workflows/ios-release-gate.yml:3-15`, `.github/workflows/ios-release-gate.yml:71-126`).
- اجتاز البنك الجديد بوابة الحقائق والمصادر والبنية كاملة: `releaseReady=true`، والتحقق الواقعي **2070/2070** مع مطابقة سجل الأدلة وبصمة المحتوى. يفشل الخادم وCI بصورة مغلقة إذا تراجعت هذه الحالة أو اختلفت البصمة.
- أضيفت اختبارات regression لإصدار البنك `v3-curated-79b8f0756254a982`: تجانس المشتتات، صحة عقد الخيارات الأربعة، إعادة حل الألغاز البوليسية، قصر «اكتشف الكلمة» على الأمثال، واستعادة منتصف الجولة بلا كشف الإجابة.

## حالة الجاهزية والمخاطر التشغيلية

الدليل الحالي **محلي فقط**. بعد مزامنة ملفات الويب مع مشروع iOS نجحت محلياً اختبارات Swift الـ32، ولكل اختبارات XCUITest الخمسة تشغيل ناجح على محاكي iPhone 17 Pro بنظام iOS 26.5، كما أُنشئ unsigned Release archive صالح يحتوي حزمة التطبيق. يلزم قبل اعتبار الإصدار production-ready:

1. تشغيل أخضر لبوابات GitHub؛ إعداد required checks وحماية الفرع خارج المستودع ولم يُتحقق منهما.
2. تنفيذ signed export/upload واختبارات TestFlight أو جهاز حقيقي لـApp Attest وDeviceCheck والشراء والاستعادة؛ لم تُنفذ هذه الخطوات ولم يدّعِ التقرير خلاف ذلك.
3. إجراء smoke test على الخادم المنشور بعد النشر، بما يشمل تحميل بنك الأسئلة وبدء جولة والاستحقاق المدفوع.

القرار الحالي لتحديث **1.4**: **التحصين البرمجي وبوابة المحتوى جاهزان للاختبار البعيد؛ وليس هناك بعدُ إثبات نشر أو توقيع أو اعتماد إنتاج نهائي على TestFlight/جهاز حقيقي.**
