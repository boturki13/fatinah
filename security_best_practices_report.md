# تقرير تنفيذ إصلاحات تحديث فطنة 1.4

تاريخ التحقق: 2026-09-09

النطاق: تطبيق iOS وSwift/WebView، خادم Python، Cloud Functions، الهوية والاشتراكات، App Attest وDeviceCheck، الخصوصية، المحتوى والصور، الاعتماديات، CI وبيانات الإصدار.

الإصدار: `1.4.0`، مرشح بناء iOS رقم `17`، المعرّف `com.fatinah.game`.

قرار البوابة الحالي: **GO مكتمل للإنتاج المنضبط على `ata20.com`، وGO للبناء 17 داخل TestFlight Internal بعد انتهاء معالجة Apple، وNO-GO مؤقت للطرح الخارجي/App Store العام حتى إكمال مصفوفة القبول والطرح التدريجي.** لا توجد موافقة في هذا التقرير على طرح التطبيق على جميع العملاء دفعة واحدة.

## الملخص التنفيذي

نُفذت الإصلاحات بمنهج fail-closed وبوابات قابلة للتكرار: حُذف ملف التشخيص الخام من النسخة الحالية، عُزل معالج Cloud Function v2، أُصلح مسار سؤال الصورة في الويب وiOS، أضيف fixture حتمي داخل `DEBUG` فقط، أصبح الخادم يتراجع إلى بنك النص إذا كانت الصور ناقصة، شُددت بيانات تتبع الإصدار، ورُفع `sharp` إلى نسخة خالية من التنبيهات المعروفة.

أثبتت بيئتا staging والإنتاج عمل الإصدار 1.4 وعقد API الصحيح، ونجحت مطابقة **600/600** أصل صورة باستخدام HTTP وMIME والحجم وSHA-256 محليًا وداخل GitHub Actions وعلى `ata20.com`. كما نجحت اختبارات Swift وXCUITest على iPhone 16 Plus حقيقي بنظام iOS 26.6.1: **29/29** اختبار أمان ووحدة و**5/5** اختبارات واجهة تشمل الإقلاع ودورة لعبة كاملة والصور والتدوير والبحث وأكبر Dynamic Type.

أُنشئ Archive للبناء 17 من المصدر الثابت `8d6eb7050f1a64213b1f1176b1be15c6191df3bf`، وصُدّر IPA موقّع بـApple Distribution مع App Attest وAPNs في بيئة الإنتاج و`get-task-allow=false`. بصمة IPA هي `958f8bfbbce973c429c07b60ac5fad0c432204e32101ec57adf4f3de3adcfd32`، وقبل App Store Connect الرفع بلا أخطاء ووضعه في المعالجة كـTestFlight Internal Testing Only.

## النسب الحالية

| المؤشر | النتيجة | التفسير |
| --- | ---: | --- |
| الأمان البرمجي القابل للإثبات | **90%** | الاختبارات والضوابط والاعتماديات قوية؛ أربعة كائنات تاريخية معروفة وبوابات التشغيل الخارجية تمنع درجة أعلى |
| جاهزية النشر التقني إلى الإنتاج | **98%** | اكتمل Republish وعقد الإنتاج وفحص الصور 600/600، وأنشئ artifact موقّع قابل للتتبع؛ تبقى مراقبة التشغيل الممتدة وتمرين rollback دوري |
| جاهزية الطرح النهائي للعملاء/App Store | **88%** | قُبل رفع TestFlight الداخلي ونجحت اختبارات جهاز حقيقي؛ تبقى معالجة Apple، جهاز/إصدار ثانٍ، واختبارات الشراء والاستعادة والهوية والحذف والشبكات الضعيفة قبل الطرح الخارجي |
| الثقة في التقرير | **مرتفعة للكود وstaging والإنتاج وiPhone الحقيقي** | تغطية آلية واسعة وفحص أصول وتوقيع كامل؛ لا يوجد اختبار اختراق مستقل أو قبول يدوي كامل على جهازين بعد |

تفصيل درجة الأمان: الهوية وسلامة الجهاز 19/20، حماية الخادم ومنع الإساءة 19/20، البيانات والخصوصية 18/20، العميل والنقل والاعتماديات 19/20، حوكمة الإصدار والأسرار 15/20 = **90/100**.

النسبة مؤشر هندسي داخلي وليست شهادة خلو من الثغرات أو ضمانًا تجاريًا.

## الإصلاحات المنفذة

### 1. سؤال الصورة وواجهة iOS

- أضيف fixture صغير وحتمي لـAVIF وWebP داخل بناء `DEBUG` فقط.
- يمر fixture عبر تحقق MIME وعدد البايتات وSHA-256 نفسه؛ لا يوجد bypass للإنتاج.
- صُححت معرفات الاختبار وحقوق الصورة وحالة فشل الإقلاع المرئية.
- نجح XCUITest `testImageQuestionLoadsInPortraitAndLandscape` على iPhone 17 Pro / iOS 26.4: اختبار واحد، صفر إخفاقات، مع لقطات مرفقة في `xcresult`.
- نجحت مجموعة XCUITest كاملة على iPhone 16 Plus حقيقي / iOS 26.6.1: خمسة اختبارات، صفر إخفاقات في جولة التثبيت النهائية. ظهر انتظار تدوير عابر في الجولة الأولى (4/5)، ثم نجح الاختبار نفسه منفردًا ونجحت المجموعة كاملة عند الإعادة؛ لم يتكرر كخلل تطبيقي.
- نجح مسار الصورة end-to-end في Chromium بلا طلب شبكة للـfixture.

### 2. Cloud Functions

- نُقل `generateQuestionsV2Handler` إلى وحدة مستقلة بلا آثار Firebase الجانبية عند الاستيراد.
- أصبحت اختبارات العقد تتحقق من 400/410 والرؤوس وتسجيل exports من دون تعليق.
- نجحت مجموعة اختبارات Cloud Functions وتدقيق اعتمادياتها بصفر ثغرات معروفة.

### 3. أصول الصور

- أضيف تحقق محلي يمنع إعلان بنك الصور جاهزًا إذا كانت الملفات ناقصة أو مخالفة للبصمة.
- عند النقص يخدم الخادم بنك النص المراجع (2,892 سؤالًا) بدل بيانات صور مكسورة.
- رُفعت حزمة staging كملف واحد لتفادي فقد الملفات المتفرق، ثم فُكت داخل المسار الصحيح.
- نجح التحقق البعيد الكامل: 300 سؤال، 600 أصل، 600/600 HTTP 200 مع MIME والحجم وSHA-256 الصحيح.
- أضيف أصل تحقق صريح لأدوات الجلب والتدقيق، مع HTTPS إلزامي ومنع بيانات الاعتماد في URL؛ يبقى `https://ata20.com` هو الافتراضي.

### 4. CI وفصل staging عن production

- يدعم التشغيل اليدوي لـGitHub Actions مدخل `verify_origin` لفحص staging قبل الإنتاج.
- تستخدم خطوات جلب الصور، التحقق من 600 أصل، والتحقق من عقد النشر الأصل نفسه.
- تستمر تشغيلات PR وmain افتراضيًا ضد `https://ata20.com` حتى لا يمر إنتاج غير مطابق.
- أزيل استيراد `functions/index.js` الجانبي من خطوة CI؛ اختبار العقد الجديد يتحقق من التسجيل بأمان.
- أضيف retry محدود ومتدرج لأخطاء الشبكة و408/429/5xx فقط؛ أي اختلاف MIME أو حجم أو SHA يفشل فورًا.
- فُصلت fixtures المنطقية الخفيفة عن فحص التوزيع الكامل، مع إبقاء اختبار الصور البعيدة end-to-end داخل بوابة الإصدار.

### 5. البيانات الحساسة وتاريخ Git

- حُذف ملف التشخيص الخام من HEAD، وأصبحت ملفات `attached_assets/Pasted-*.txt` محظورة في `.gitignore`.
- حُسن ماسح الأسرار لفحص الملفات الحالية المعدلة مع تاريخ Git.
- تبقى أربعة كائنات تاريخية معروفة ومقر بها. لم يُنفذ history rewrite أو force-push لأنه إجراء مدمر يحتاج خطة تدوير وتنسيقًا منفصلًا.

### 6. تتبع الإصدار والاعتماديات

- رُفع مرشح iOS إلى build 17 ومنع إعادة استخدام بناء غير قابل للإثبات.
- أصبحت `release/current.json` على schema 2، وحالتها `testflight_uploaded_processing` مع source SHA وبصمة artifact فعليين.
- يمنع الاختبار ادعاء TestFlight من دون source SHA بطول 40 وبصمة artifact بطول 64.
- رُفع `sharp` إلى `0.35.4`؛ تدقيق npm للمشروع وCloud Functions أصبح صفرًا من الثغرات المعروفة.

## أدلة التحقق

| البوابة | النتيجة |
| --- | --- |
| `npm run test:ci` | ناجح، exit 0 |
| `npm run test:static` | ناجح |
| اختبارات الويب والحساب واللعب والمحتوى والخادم | ناجحة |
| تدقيق npm للمشروع وCloud Functions | 0 ثغرات معروفة |
| فحص الصور المحلي | ناجح: 300 سؤال و600 أصل |
| عقد نشر staging | ناجح: 1.4.0، 3,192 سؤالًا، 40 فئة، contract revision الصحيح |
| أصول staging البعيدة | ناجح: 600/600 مع MIME والحجم وSHA-256 |
| XCUITest لمسار الصورة | ناجح على المحاكي: 1/1 وصفر إخفاقات |
| GitHub Actions على commit `8d6eb70` | ناجح: Core + Release asset integrity، التشغيل `34359386512` |
| GitHub Actions بعد النشر ضد `ata20.com` | ناجح: Core + Release asset integrity، التشغيل `34364501251` |
| GitHub Actions لفرع البناء 17 | ناجح كاملًا في المحاولة الثانية بعد مهلة عابرة باختبار الاستعادة؛ التشغيل `34371345582` |
| عقد نشر `ata20.com` بعد Republish | ناجح: production، 1.4.0، 3,192 سؤالًا، 40 فئة، `fatinah-v2-2026-09-08` |
| أصول `ata20.com` بعد Republish | ناجح: 600/600 مع HTTP وMIME والحجم وSHA-256؛ البنك `images-v4-curated-c3b9161304253db6` |
| Swift Testing على جهاز حقيقي | ناجح: 29/29 ضمن App Attest وDeviceCheck وKeychain وMetricKit، iPhone 16 Plus / iOS 26.6.1 |
| XCUITest على جهاز حقيقي | ناجح نهائيًا: 5/5؛ الإقلاع، الجولة الكاملة، الصور عمودي/أفقي، البحث، وأكبر Dynamic Type |
| Archive وIPA للبناء 17 | ناجح: Apple Distribution، توقيع صالح، `get-task-allow=false`، App Attest وAPNs production، وبصمة SHA-256 مسجلة |
| رفع TestFlight الداخلي | ناجح بلا أخطاء؛ App Store Connect upload `043ca22d-1849-4302-bb7d-f0492ef14737`، والحالة بانتظار المعالجة |

## الموانع المتبقية

اكتمل نشر الإنتاج والتحقق بعده في 2026-09-09: عقد الإصدار ناجح وجميع الأصول 600/600 مطابقة، لذلك لم يُفعّل مسار rollback.

### مكتمل — بناء App Store قابل للتتبع

أُنجز signed archive من `sourceCommit` المثبت، وحُسبت SHA-256 وسُجلت في بيانات الإصدار، وقبل App Store Connect الرفع بلا أخطاء. الرفع مقيّد حاليًا بـInternal Testing Only ولا يمثل طرحًا خارجيًا أو إصدار App Store عامًا.

### P0 — معالجة Apple وقبول العملاء قبل الطرح الخارجي

يلزم أولًا اكتمال معالجة Apple وظهور البناء في TestFlight. اكتمل القبول الآلي على جهاز حقيقي واحد، ويبقى جهاز/إصدار مدعوم ثانٍ واختبار يدوي يشمل: شبكة ضعيفة وأوفلاين، شراء واستعادة sandbox، تسجيل Apple/Google/الهاتف، حذف الحساب، VoiceOver، ومراقبة Crashlytics/MetricKit قبل توسيع النسبة.

### P1 — تاريخ Git والأسرار

يلزم تقييم الكائنات التاريخية الأربعة وتدوير أي سر متأثر، ثم history rewrite منسق ونسخة احتياطية وإعادة clone للمتعاونين. حذف الملف من HEAD وحده لا ينظف التاريخ.

### P2 — بنك الإصدار التالي

لقطة baseline المستقبلية `gulf-pre-release-baseline-...-2070.json` غير موجودة. لا يجوز اختلاقها أو تعطيل شرط البصمة؛ هذا لا يمنع بنك تشغيل 1.4 الحالي لكنه يمنع اعتماد بنك الإصدار التالي.

## الحكم

**إصلاحات الكود وبيئة staging ونشر `ata20.com` والبناء الموقع واختبارات iPhone الحقيقي نُفذت ونجحت، ودرجة الأمان القابلة للإثبات 90%. البناء 17 مقبول للرفع وموجود في معالجة TestFlight الداخلية، لذلك قرار التشغيل الحالي GO للإنتاج وGO داخلي بعد المعالجة. يبقى الطرح الخارجي/App Store العام في حالة NO-GO حتى تنتهي معالجة Apple ومصفوفة القبول على جهاز ثانٍ ومسارات الشراء والهوية والحذف والشبكات الضعيفة، ثم يبدأ rollout تدريجي قابل للإيقاف والرجوع.**
