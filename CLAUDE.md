# مشروع فَطِنة (Fatinah) — دليل Claude Code

هذا الملف يقرأه Claude Code في بداية كل جلسة. يحتوي كل ما تحتاج معرفته عن المشروع.

## نظرة عامة

**فَطِنة** لعبة مسابقات جماعية عربية بلهجة خليجية، موجّهة للعائلات والمجالس. الهدف: نشرها على App Store كتطبيق iOS/iPadOS عالي الجودة.

- **النوع:** لعبة معرفة شفهية (verbal trivia) — الأسئلة تظهر بلا خيارات، والفرق تجيب شفهياً، والمقدّم يحكم.
- **البنية الحالية:** تطبيق ويب (HTML/CSS/JS في ملف واحد) مغلّف عبر Capacitor داخل مشروع iOS.
- **اللغة:** عربي بلهجة خليجية، واجهة RTL.
- **الحالة:** بعد جولة تدقيق شاملة (audit) وإصلاح 6 مشاكل حرجة (انظر "آخر التحديثات" أدناه). لا تزال هناك مهام قبل النشر — راجع "مهام مستقبلية".

## البنية التقنية

- **الغلاف:** Capacitor 6 (يحوّل تطبيق الويب إلى تطبيق iOS أصلي).
- **قلب التطبيق:** `www/index.html` — ملف واحد فيه كل اللعبة (HTML + CSS + JavaScript، بدون اعتماديات خارجية عدا خط Google Fonts).
- **لا يوجد backend:** كل شيء يعمل محلياً على الجهاز. بنك الأسئلة مضمّن داخل الملف كـ JavaScript object (12 فئة، 231 سؤالاً حالياً).
- **التخزين:** ✅ دائم فعلياً الآن عبر `localStorage` + جسر اختياري لـ Capacitor Preferences (دوال `storeGet`/`storeSet` في أعلى الملف). يحفظ: الإحصاءات، الإنجازات، الأسئلة العائلية، اسم اللاعب، إعداد الصوت.

## آخر التحديثات (جولة إصلاحات ما بعد التدقيق)

تم إصلاح 6 مشاكل حرجة كانت ستمنع قبول التطبيق في App Store أو تكسر التجربة:

1. **التخزين الدائم** — كان كل شيء يُفقد عند إغلاق التطبيق (متغيرات ذاكرة فقط). الآن `storeGet`/`storeSet` يحفظان في `localStorage` فعلياً، مع مزامنة اختيارية لـ Capacitor Preferences عند توفّره.
2. **تكرار الأسئلة داخل الجولة** — كان `pickQuestion` يسحب عشوائياً بلا استبعاد. الآن `state.usedQ` (Set) يمنع تكرار أي سؤال داخل نفس الجولة، مع سقوط منطقي عند نفاد المستوى/الفئة.
3. **عدالة السرقة مع 3 فرق** — كان الفريق الثالث مستبعداً تماماً من السرقة (منطق "خصم واحد ثابت"). أُعيد تصميم النظام بـ `stealQueue` (طابور يشمل كل الفرق الأخرى بالتناوب) — كل فريق يُطرح عليه السؤال بدوره.
4. **أزرار الدخول الوهمية** — كانت أزرار Apple/Google/WhatsApp تدّعي مصادقة حقيقية بلا تنفيذ. استُبدلت بتسجيل دخول حقيقي (انظر القسم التالي).
5. **الاهتزاز على iOS** — `navigator.vibrate` لا يعمل في WKWebView. دالة `vibrate()` الآن تستخدم `Capacitor.Plugins.Haptics` عند توفّره، مع `navigator.vibrate` كاحتياط للمتصفح.
6. **إطفاء الشاشة أثناء السؤال** — أُضيفت `keepAwakeOn()`/`keepAwakeOff()` مربوطة بفتح/إغلاق السؤال، تستخدم `Capacitor.Plugins.KeepAwake` عند توفّره، أو Screen Wake Lock API كاحتياط.

**بالإضافة:** أُصلح خطأ توقيت — كانت `renderLifelines()` تُستدعى قبل `startPhase('owner')` فتُخفي وسيلة "مرّرها للخصم" وتُعطّل كل الوسائل في اللحظة الأولى لكل سؤال. أُصلح ترتيب الاستدعاء.

**تطوير المحتوى:** أُضيفت فئة "فتوحات المسلمين" (30 سؤالاً محقّقاً بالبحث من مصادر تاريخية إسلامية متعددة، متدرّجة الصعوبة).

## تسجيل الدخول الحقيقي (Apple + Google)

**لا يوجد واتساب كـ "تسجيل دخول"** — حقيقة تقنية: ميتا لا توفّر بوابة "Sign in with WhatsApp" رسمية للمستخدم العادي (فقط WhatsApp Business API المدفوع للتحقق برقم الهاتف، غير مناسب هنا). لذا الدخول مبني على مزوّدين فقط: Apple وGoogle، بالإضافة لخيار "متابعة بالاسم" الذي يعمل دائماً بلا أي إعداد.

- **المكوّن:** `@capacitor-firebase/authentication` (مكوّن موحّد لـ Apple وGoogle معاً عبر Firebase، بدل مكوّنين منفصلين).
- **الكود:** دوال `appleSignIn()` / `googleSignIn()` في `www/index.html`، تتحقق من وجود `window.Capacitor.Plugins.FirebaseAuthentication` وتتدهور بلطف (تنبيه Toast) إن لم يتوفّر — لا انهيار أبداً في المتصفح أو قبل إكمال الإعداد.
- **الإعداد المطلوب على جهازك (لا يمكن فعله عن بُعد):** إنشاء مشروع Firebase، تفعيل مزوّدَي Apple وGoogle فيه، إضافة `GoogleService-Info.plist` للمشروع، وتفعيل قدرة "Sign in with Apple" في Xcode. **الخطوات الكاملة في `دليل-التثبيت.md` → الجزء الرابع.**
- **بعد الدخول بأي طريقة:** يُستدعى `completeLogin(name, provider, uid)` الذي يوحّد الحفظ (اسم اللاعب + المزوّد + معرّف المستخدم إن وُجد) عبر نظام التخزين الدائم.

## ملفات المشروع

```
fatinah-ios/
├── www/index.html          ← كل اللعبة هنا (لا تعدّل غيره إلا للضرورة)
├── capacitor.config.ts     ← إعدادات Capacitor (appId, ألوان, plugins)
├── package.json            ← الاعتماديات (يشمل الآن @capacitor/preferences و keep-awake)
├── CLAUDE.md               ← هذا الملف
└── ios/                    ← يُنشأ تلقائياً بعد `npx cap add ios`
    └── App/App/            ← مشروع Xcode الفعلي
```

## تشغيل التطبيق على Replit (ويب)

```bash
python3 server.py
```

يشتغل على http://0.0.0.0:5000 — يخدم `index.html`، ويوفّر `/api/generate` (Claude) و`/firebase-config.js`.

**المتغيّرات البيئية المطلوبة:**
- `ANTHROPIC_API_KEY` — لتوليد الأسئلة بالذكاء الاصطناعي (Claude opus-4-5)
- `FIREBASE_API_KEY`, `FIREBASE_AUTH_DOMAIN`, `FIREBASE_PROJECT_ID`, `FIREBASE_APP_ID`, `FIREBASE_MESSAGING_SENDER_ID` — اختيارية، تُفعّل Google/Apple sign-in في المتصفح

## أوامر البناء والتشغيل (iOS)

```bash
# أول مرة فقط: تثبيت الاعتماديات وإضافة منصة iOS
npm install
npx cap add ios
npx cap sync ios

# بعد أي تعديل على index.html:
npx cap sync ios        # ينسخ ملفات الويب إلى مشروع iOS
npx cap open ios        # يفتح المشروع في Xcode

# داخل Xcode: اختر جهازاً/محاكياً ثم اضغط Run (⌘R)
```

**مهم:** أي تعديل على اللعبة يكون في `index.html`، ثم `npx cap sync ios` لنقله.

**تفعيل Haptics/KeepAwake الأصليين فعلياً:** الكود يتحقّق تلقائياً من وجود `window.Capacitor.Plugins.Haptics` و`window.Capacitor.Plugins.KeepAwake`. بعد `npm install` و`npx cap sync ios`، ستُستخدم النسخة الأصلية تلقائياً بلا أي تعديل إضافي على الكود.

## معلومات App Store

- **appId:** `com.fatinah.game` (غيّره إن أردت نطاقك الخاص)
- **اسم التطبيق:** فَطِنة
- **لون الخلفية/الحالة:** `#120B24` (كحلي/بنفسجي عميق — ثيم اللعبة الداكن)
- **الاتجاه:** يدعم Portrait و Landscape (اللوحة تتكيّف).
- **متطلبات مطوّر آبل:** حساب Apple Developer (99$/سنة) للنشر.

## مهام مستقبلية مقترحة (بالأولوية بعد الإصلاحات الستة)

1. **اختبار على جهاز iOS حقيقي** — كل الإصلاحات (خصوصاً Haptics وKeepAwake وlocalStorage) اختُبرت في متصفح Chromium فقط حتى الآن. WKWebView الحقيقي قد يتصرّف مختلفاً. **إلزامي قبل أي نشر.**
2. **الأيقونة وشاشة البداية** — تصميم App Icon (1024×1024) وSplash Screen بشعار اللعبة (حالياً لون خلفية فقط بلا شعار).
3. **Privacy Manifest + سياسة الخصوصية** — إلزامية من آبل، غير موجودة بعد.
4. **مراجعة/توسيع الفئات القديمة الثماني** (معلومات عامة، رياضة، تاريخ، جغرافيا، أمثال، ثقافة خليجية، دين وسيرة، علوم وتقنية) — 12 سؤالاً فقط لكل واحدة مقابل 30-39 للفئات الجديدة المحقّقة. تحتاج نفس معيار الجودة والعدد.
5. **توسيع مجموعة الإسلاميات** — الفئات القادمة: القرآن (مكتمل)، السيرة (مكتملة)، الفتوحات (مكتملة) ← التالي: الصحابة، الخلفاء الراشدون، الأنبياء والرسل.
6. ~~Sign in with Apple حقيقي~~ — ✅ منجَز (Apple + Google عبر Firebase). المتبقّي فقط: إكمال إعداد Firebase على جهازك (دليل-التثبيت.md → الجزء الرابع) واختبار على جهاز حقيقي.
7. **Crashlytics/Analytics أساسية** — لا رصد أخطاء أو استخدام بعد النشر حالياً.

## قواعد مهمة عند التعديل

- **لا تكسر RTL:** كل الواجهة عربية من اليمين لليسار.
- **حافظ على الثيم الداكن:** المتغيرات في `:root` (ألوان `--bg`, `--fire1..6`, `--hot`).
- **الأسئلة الدينية حسّاسة:** أي سؤال إسلامي يجب التحقّق من صحته من مصدر موثوق قبل إضافته. تجنّب المسائل الخلافية بين المؤرخين (مثل تواريخ مختلف عليها) إلا بصياغة تراعي الخلاف.
- **اختبر بعد كل تعديل:** ابنِ في المحاكي وتأكد أن جولة كاملة (فريقين وثلاثة) تعمل من الدخول حتى النتيجة، وأن `state.usedQ` يُصفَّر بشكل صحيح مع أي مسار بدء جولة جديد تضيفه.
- **بنية السؤال:** يُفضَّل النظام الخفيف `{d: مستوى الصعوبة 1-6, q: "السؤال", answer: "الإجابة"}`. النظام القديم بخيارات `{o: [...], a: مؤشر}` لا يزال مدعوماً للتوافق العكسي.
- **عند إضافة مسار جديد يبدأ جولة** (غير `startGame`/`playFamilyRound` الموجودين): لا تنسَ `state.usedQ=new Set()`.

## آلية اللعبة (مرجع سريع)

- فريقان أو ثلاثة، لوحة فئات × ٦ مستويات صعوبة (١٠٠-٦٠٠ نقطة).
- عدد الفئات ٤-١٠ (منزلق)، تُختار بالتناوب بين الفرق (كل فريق يكمل نصيبه قبل أن ينتقل الدور).
- مؤقّت ٦٠ث لصاحب الدور، ٣٠ث لكل فريق في طابور السرقة (يدعم أي عدد فرق، لا يقتصر على فريقين).
- ٤ وسائل لكل فريق: بحث بالجوال، مرّرها للخصم (يُسقط حق السرقة عن المالك، ينقل السؤال مباشرة لأول فريق في الطابور)، تغيير السؤال، مضاعفة النقاط.
- الإجابة الشفهية: تظهر بعد طرح السؤال على كل الفرق المؤهّلة، والمقدّم يحكم بأي فريق يأخذ النقاط (أو لا أحد).

## أفكار مُلغاة (لا تُقترح مجدداً)

- **خمن الصوت**: فئة أصوات مركَّبة برمجياً (Web Audio). أُلغيت — جودة التوليد غير مقنعة لأصوات معقّدة كالطائرات.
- **ماركات (شعارات سيارات)**: فئة صور شعارات شركات. أُلغيت — الشعارات علامات تجارية محمية، القيد يبقى حتى لو المصدر Kaggle أو غيره. البديل المقبول: وصف الشعار نصّياً بدل عرضه (نفس أسلوب فئة أعلام الدول).
