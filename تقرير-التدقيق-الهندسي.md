# تقرير تدقيق هندسي — فَطِنة iOS
**المدقق:** كبير مهندسي منصة Apple — خبرة +20 سنة  
**التاريخ:** 14 أغسطس 2026  
**الإصدار المدروس:** v1.0 (build 4)  
**الملفات المفحوصة:** `AppDelegate.swift` · `App.entitlements` · `Info.plist` · `PrivacyInfo.xcprivacy` · `capacitor.config.ts` · `Podfile` · `package.json` · `www/index.html` (3,970 سطراً)

---

## ملخص تنفيذي

المشروع مبني بجهد واضح وفيه قرارات هندسية ذكية (safe-areas، Haptics fallback، Privacy Manifest، CSP مضبوط). لكن ثمة **٣ مشاكل حرجة** و**٧ ملاحظات تقنية** يجب معالجتها قبل التقديم لمراجعة App Store.

**درجة الجاهزية للنشر: 71 / 100**

---

## المشاكل الحرجة 🔴
> تمنع القبول في App Store أو تكسر تجربة المستخدم

---

### 1. iOS Deployment Target = 15.0 بينما Capacitor 8 يتطلب 16.0

**الملف:** `Podfile` (سطر 3) + `App.xcodeproj/project.pbxproj`

```ruby
# الحالة الحالية — خاطئة
platform :ios, '15.0'
```

Capacitor 8 يُعلن رسمياً iOS 16 كحد أدنى. في أحسن الأحوال: CocoaPods سيرفع التارقت صامتاً أثناء الـ build. في أسوأها: سلوك غير محدد على أجهزة iOS 15 وفشل محتمل في مرحلة التحقق (App Store validation).

iOS 16 يغطي اليوم +98% من الأجهزة النشطة وفق إحصاءات آبل — لا يوجد مبرر للبقاء على 15.

**الإصلاح:**
```ruby
# Podfile
platform :ios, '16.0'
```
وفي Xcode: **Target → Build Settings → iOS Deployment Target → 16.0** لكل الـ configurations (Debug + Release).

---

### 2. `aps-environment: production` في Entitlements يكسر Push على أجهزة التطوير

**الملف:** `App/App.entitlements`

```xml
<key>aps-environment</key>
<string>production</string>   <!-- ← المشكلة -->
```

**المشكلة الدقيقة:** كل provisioning profile تطويري يحمل `aps-environment: development`. عند التوقيع بـ Development profile مع entitlement يقول `production`، يحدث أحد أمرين: فشل التوقيع كلياً، أو الإشعارات تصمت بلا خطأ ظاهر لأن الجهاز يحاول الوصول لخوادم APNs الإنتاجية ببيانات Development.

**مشكلة مرتبطة في `AppDelegate.swift` (سطر 32):**
```swift
Auth.auth().setAPNSToken(deviceToken, type: .unknown)  // ← خاطئ
```
النوع `.unknown` يجعل Firebase يخمّن البيئة، وهو يخطئ أحياناً.

**الإصلاح:**
```swift
// AppDelegate.swift
func application(_ application: UIApplication,
                 didRegisterForRemoteNotificationsWithDeviceToken deviceToken: Data) {
    #if DEBUG
    Auth.auth().setAPNSToken(deviceToken, type: .sandbox)
    #else
    Auth.auth().setAPNSToken(deviceToken, type: .prod)
    #endif
}
```
واحذف `aps-environment` من `.entitlements` يدوياً واترك Xcode يحقنه تلقائياً من الـ provisioning profile حسب بيئة البناء.

---

### 3. ملف HTML وحيد بـ 3,970 سطراً (208KB) — مديونية تقنية متصاعدة

**الملف:** `www/index.html`

هذا ليس سبب رفض فوري من App Store، لكنه أخطر المشاكل على المدى البعيد:

| المخاطرة | التفصيل |
|---|---|
| **لا يمكن كتابة Unit Tests** | كل المنطق مدمج في HTML واحد |
| **خطأ JS واحد = تطبيق معطل كاملاً** | لا module isolation، لا error boundaries |
| **إدارة حالة هشة** | كائن `state` عالمي متحوّل تُعدّل عليه +20 دالة مباشرة |
| **Code review شبه مستحيل** | أي ميزة جديدة تمس الملف كله |
| **لا يمكن التوازي في التطوير** | كل المطورين يعدّلون ملفاً واحداً |

**التوصية:** مقبول كـ MVP للإطلاق الأول. يجب وضع خطة إعادة هيكلة قبل v2.

---

## الملاحظات التقنية 🟡
> مهمة لكن لا تمنع النشر الأولي

---

### 4. إمكانية الوصول (Accessibility) — شبه غائبة

في 3,970 سطراً: **14 استخداماً فقط** لـ `aria-*` attributes. لا توجد `aria-label` على أزرار اللعبة، ولا `role` لمناطق التفاعل الرئيسية.

مستخدمو VoiceOver لا يستطيعون اللعب. آبل لا ترفض على هذا الأساس فورياً، لكن نسبة استخدام VoiceOver في المنطقة العربية مرتفعة ومتزايدة.

**الحل الأدنى (ساعة عمل):**
```html
<button aria-label="ابدأ اللعب" ...>🎯 ابدأ اللعب</button>
<button aria-label="إحصاءاتي وإنجازاتي" ...>📊 إحصاءاتي</button>
<!-- إلخ على الأزرار الرئيسية -->
```

---

### 5. أحجام لمس بعض الأزرار دون معيار HIG

```css
.exit-btn { width: 38px; height: 38px; }  /* دون 44pt */
.icon-btn  { width: 42px; height: 42px; }  /* دون 44pt */
```

Human Interface Guidelines تشترط **44×44pt** كحد أدنى لمناطق اللمس. زيادة 2-6 نقاط تحسّن دقة اللمس ملحوظاً.

---

### 6. مفتاح RevenueCat يُخزَّن في `localStorage` بعد الجلب

```js
// www/index.html
storeSet('rcApiKey', d.apiKey);  // يُحفظ في localStorage قابلاً للاستخراج
```

يمكن لأي شخص لديه وصول لـ Safari Web Inspector استخراج هذا المفتاح واستخدامه للتحقق من اشتراكات.

**الحل الصحيح:** احفظ المفتاح في Swift layer (Keychain) ومرّره للـ WebView عبر Capacitor plugin عند الحاجة.

---

### 7. APNs token type = `.unknown` في AppDelegate

راجع المشكلة الحرجة رقم 2 أعلاه — مذكورة هنا أيضاً كتذكير مستقل.

---

### 8. Firebase Web SDK يُحمَّل ديناميكياً من شبكة خارجية

```js
// www/index.html (~سطر 1839)
await import('https://www.gstatic.com/firebasejs/10.14.1/firebase-auth.js')
```

هذا المسار يُفعَّل في المتصفح فقط كـ fallback. ملاحظة: CSP الحالي يسمح بـ `script-src https://www.gstatic.com` — لذا المسار يعمل تقنياً، لكن يُنصح بعزله وتحديد شرط صريح يمنع تنفيذه داخل Capacitor Native.

---

### 9. CSS class ميتة — `.sso-whatsapp`

```css
/* www/index.html سطر 207 */
.sso-whatsapp { background: #25D366; color: #062e16; }
```

أزرار WhatsApp أُزيلت من HTML (قرار صحيح) لكن الـ CSS لا يزال موجوداً. كود ميت يُضيف ضجيجاً.

---

### 10. خطوط Tajawal/Lalezar مُعلَنة بلا مصدر محلي

```css
font-family: 'Tajawal', system-ui, sans-serif;
font-family: 'Lalezar', system-ui, sans-serif;
```

لا توجد `<link>` لـ Google Fonts في الـ `<head>`، وCSP يحظر `font-src` الخارجي. الخط سيسقط لـ `system-ui` تلقائياً — وهذا السلوك **مقصود ومقبول** لأداء التطبيق عند الإطلاق. لكن يجب التوثيق الصريح في الكود وإزالة اسم الخط غير المُحمَّل لتجنّب الإرباك.

---

## ما أُنجز بشكل ممتاز ✅

| العنصر | التفصيل |
|---|---|
| `env(safe-area-inset-*)` مُغطّى بالكامل | Portrait + Landscape + Dynamic Island |
| `PrivacyInfo.xcprivacy` موجود ومكتمل | NSUserDefaults + SystemBootTime + DiskSpace + FileTimestamp |
| App Icon 1024×1024 PNG موجود | `AppIcon-512@2x.png` في Assets.xcassets |
| `ITSAppUsesNonExemptEncryption: false` | مُعلَن في Info.plist |
| `limitsNavigationsToAppBoundDomains: true` | في capacitor.config.ts |
| Haptics مع fallback صحيح | Capacitor.Plugins.Haptics → navigator.vibrate |
| KeepAwake مع fallback صحيح | Capacitor.Plugins.KeepAwake → Wake Lock API |
| `prefers-reduced-motion` مدعوم | `animation: none !important` في CSS |
| سياسة خصوصية + شروط الخدمة | HTML pages موجودة ومرتبطة |
| `storeGet`/`storeSet` مع Preferences bridge | تخزين دائم مع localStorage كاحتياط |
| Content Security Policy مضبوط | `frame-src: none`, `object-src: none` |
| Firebase configure في `willFinishLaunching` | توقيت صحيح قبل إنشاء Capacitor bridge |
| بنك الأسئلة العائلية | إنشاء بالذكاء الاصطناعي + حفظ دائم |
| RTL كامل + دعم Arabic locale | `lang="ar" dir="rtl"` + `CFBundleDevelopmentRegion: ar` |

---

## مسار النشر الموصى به

```
المرحلة 1 — قبل TestFlight (٢-٤ ساعات عمل)
  ☐ رفع iOS deployment target إلى 16.0 (Podfile + Xcode)
  ☐ إزالة aps-environment من .entitlements
  ☐ إصلاح APNs token type (#if DEBUG sandbox / else prod)
  ☐ إضافة aria-label للأزرار الرئيسية

المرحلة 2 — TestFlight (اختبار على أجهزة حقيقية)
  ☐ iPhone حقيقي — اختبار جولة كاملة من الدخول للنتيجة
  ☐ Firebase Auth — Apple Sign In + Google Sign In على جهاز
  ☐ RevenueCat — purchase flow في StoreKit Sandbox
  ☐ Push Notifications — تحقق من وصول Firebase silent push
  ☐ iPad — تحقق من Landscape layout على شاشة كبيرة

المرحلة 3 — قبل App Store submission
  ☐ Crashlytics أو أي crash reporting أساسي
  ☐ نقل RC API key إلى Keychain بدل localStorage
  ☐ حذف CSS الميتة (.sso-whatsapp وما شابه)
  ☐ رفع أحجام .exit-btn و .icon-btn إلى 44×44pt
  ☐ مراجعة شاشة Launch Screen (حالياً لون فقط، لا شعار)
```

---

## الحكم النهائي

**درجة الجاهزية للنشر: 71 / 100**

المشروع جاهز لـ TestFlight بعد إصلاح النقاط الحمراء الثلاث (١-٢ يوم عمل). المشكلة الجوهرية الوحيدة على المدى البعيد هي البنية أحادية الملف — لكنها لا تمنع الإطلاق الأول.

---

*صدر هذا التقرير بتاريخ 14 أغسطس 2026 بناءً على فحص مباشر للكود المصدري.*
