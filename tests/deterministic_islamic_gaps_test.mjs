import assert from'node:assert/strict';import fs from'node:fs';const c=JSON.parse(fs.readFileSync(new URL('../content/questions/candidates.json',import.meta.url),'utf8')),g=c.filter(x=>x.generation?.model==='deterministic-islamic-reference-template-v1');assert.equal(g.length,21);assert.ok(g.every(x=>x.religious&&x.review.religiousSourceAndIsnadConfirmed&&x.review.religiousNoDisputedMatterConfirmed));assert.ok(g.every(x=>x.cost.runEstimatedUsd===0));console.log('✓ 21 سؤال إسلاميات مرجعي من القرآن أو المتفق عليه وتكلفة AI صفر');
